# Student Todo Planner 项目说明与面试问答

## 项目内容

这是一个面向学生的纯前端待办应用。用户可以添加、完成和删除任务，根据学习目标生成五步计划，并通过统计卡片和柱状图查看进度。数据保存在浏览器 `localStorage` 中。

## 我做了什么

- 将需求拆分为任务数据、界面渲染、用户操作和持久化四部分。
- 使用 DOM API 根据数组状态重新渲染界面。
- 用 JSON 序列化任务，存入 `localStorage`。
- 补充键盘操作、ARIA 标签和输入校验。
- 使用 Git 记录修复，并完善 README。

## 面试官可能问什么

### 为什么选这个项目

我想从自己真正会用的工具开始，它的功能边界清楚，但又能练到数据、交互、调试和交付的完整流程。

### 数据保存在哪里

保存在浏览器的 `localStorage`。优点是无需服务器、实现简单；局限是数据只属于当前浏览器，无法跨设备同步。

### 如果要增加后端会怎么做

我会设计任务 API，例如 `GET /tasks`、`POST /tasks`、`PATCH /tasks/:id`和 `DELETE /tasks/:id`，后端再把数据写入数据库。

### AI 在项目中起了什么作用

AI 帮我拆分功能、检查边界情况和改善无障碍细节。我会阅读修改、用实际操作验证，并能解释关键代码，不是直接接受输出。

### 遇到的主要问题是什么

早期版本刷新后任务会丢失，而且仓库没有标准的 `index.html` 入口。我增加了本地持久化，并把页面改为标准静态站点入口。

## 演示顺序

1. 添加一个任务。
2. 点击柱子完成任务，观察进度变化。
3. 生成学习计划。
4. 刷新页面，证明数据仍然存在。
5. 打开 Git 提交记录，说明一次修复的过程。
